import { Archive, CreditCard, Landmark, WalletCards } from "lucide-react";
import { getCurrentHousehold } from "@/lib/household";
import { formatMoney } from "@/lib/finance/money";
import { archiveAccount, createAccount } from "./actions";

type AccountRow={id:string;name:string;type:string;current_balance_cents:number;is_shared:boolean};
export default async function AccountsPage(){const {supabase,household}=await getCurrentHousehold();if(!household)return null;const {data}=await supabase.from("accounts").select("id,name,type,current_balance_cents,is_shared").eq("household_id",household.id).is("archived_at",null).order("name");const accounts=(data??[]) as AccountRow[];return <><h1 className="text-3xl font-black">Cuentas</h1><p className="mt-2 text-[#6c7f7a]">El dinero que gestionáis en el hogar.</p><div className="mt-7 grid gap-4 sm:grid-cols-2 lg:grid-cols-3">{accounts.map(a=><article className="card p-5" key={a.id}><div className="flex items-center justify-between"><span className="grid size-11 place-items-center rounded-xl bg-[#dceee6]">{a.type==="cash"?<WalletCards/>:a.type==="card"?<CreditCard/>:<Landmark/>}</span><span className="rounded-full bg-[#eef2ef] px-2.5 py-1 text-xs font-bold">{a.is_shared?"Compartida":"Personal"}</span></div><h2 className="mt-5 font-black">{a.name}</h2><div className="flex items-end justify-between"><p className="mt-1 text-2xl font-black">{formatMoney(a.current_balance_cents)}</p>{!a.is_shared&&<form action={archiveAccount}><input type="hidden" name="id" value={a.id}/><button aria-label={`Archivar ${a.name}`} className="rounded-lg p-2 text-[#9aaaa5] hover:bg-red-50 hover:text-red-600"><Archive size={17}/></button></form>}</div></article>)}</div><section className="card mt-7 max-w-2xl p-6"><h2 className="text-lg font-black">Añadir cuenta personal</h2><form action={createAccount} className="mt-5 grid gap-4 sm:grid-cols-2"><label><span className="label">Nombre</span><input className="field" name="name" required placeholder="Mi tarjeta"/></label><label><span className="label">Tipo</span><select className="field" name="type"><option value="bank">Cuenta bancaria</option><option value="card">Tarjeta</option><option value="cash">Efectivo</option><option value="savings">Ahorros</option></select></label><label><span className="label">Saldo inicial</span><input className="field" name="balance" inputMode="decimal" placeholder="0,00"/></label><button className="self-end rounded-xl bg-[#26725c] px-5 py-3 font-bold text-white">Guardar cuenta</button></form></section></>}
