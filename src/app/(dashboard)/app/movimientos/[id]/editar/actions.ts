"use server";
import { redirect } from "next/navigation";
import { getCurrentHousehold } from "@/lib/household";
import { eurosToCents } from "@/lib/finance/money";
export async function editTransaction(formData:FormData){const {supabase,household}=await getCurrentHousehold();if(!household)throw new Error("Sin hogar");const id=String(formData.get("id"));const description=String(formData.get("description")??"").trim();const scope=String(formData.get("scope"));const privacy=String(formData.get("privacy"));const accountId=String(formData.get("accountId"));const categoryId=String(formData.get("categoryId"));const transactionDate=String(formData.get("transactionDate"));const amountCents=eurosToCents(String(formData.get("amount")));if(description.length<2||!["personal","shared"].includes(scope)||!["visible","private"].includes(privacy)||(scope==="shared"&&privacy==="private"))throw new Error("Datos no válidos");const [{data:account},{data:category}]=await Promise.all([supabase.from("accounts").select("id").eq("id",accountId).eq("household_id",household.id).maybeSingle(),supabase.from("categories").select("id").eq("id",categoryId).maybeSingle()]);if(!account||!category)throw new Error("Cuenta o categoría no válida");const {error}=await supabase.rpc("update_financial_transaction",{p_transaction_id:id,p_account_id:accountId,p_amount_cents:amountCents,p_description:description,p_category_id:categoryId,p_scope:scope,p_privacy:privacy,p_transaction_date:transactionDate});if(error)throw new Error(error.message);redirect("/app/movimientos")}
