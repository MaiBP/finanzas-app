import { notFound } from "next/navigation";
import { getCurrentHousehold } from "@/lib/household";
import { editTransaction } from "./actions";
import { SubmitButton } from "@/components/ui/submit-button";
type Row={id:string;description:string;amount_cents:number;type:string;scope:string;privacy:string;transaction_date:string;account_id:string;category_id:string};
export default async function EditPage({params}:{params:Promise<{id:string}>}){const {id}=await params;const {supabase,user,household}=await getCurrentHousehold();if(!household)return null;const [{data},{data:accounts},{data:categories}]=await Promise.all([supabase.from("transactions").select("id,description,amount_cents,type,scope,privacy,transaction_date,account_id,category_id").eq("id",id).eq("created_by",user.id).eq("status","confirmed").maybeSingle(),supabase.from("accounts").select("id,name").eq("household_id",household.id).is("archived_at",null),supabase.from("categories").select("id,name,kind").or(`household_id.eq.${household.id},household_id.is.null`).order("name")]);if(!data)notFound();const row=data as Row;return <div className="mx-auto max-w-2xl"><h1 className="text-3xl font-black">Editar movimiento</h1><section className="card mt-7 p-6"><form action={editTransaction} className="space-y-4"><input type="hidden" name="id" value={id}/><label><span className="label">Descripción</span><input className="field" name="description" defaultValue={row.description} required/></label><div className="grid grid-cols-2 gap-3"><label><span className="label">Importe</span><input className="field" name="amount" defaultValue={(row.amount_cents/100).toFixed(2).replace(".",",")} required/></label><label><span className="label">Fecha</span><input className="field" type="date" name="transactionDate" defaultValue={row.transaction_date} required/></label></div><div className="grid grid-cols-2 gap-3"><label><span className="label">Cuenta</span><select className="field" name="accountId" defaultValue={row.account_id}>{(accounts??[]).map(a=><option value={a.id} key={a.id}>{a.name}</option>)}</select></label><label><span className="label">Categoría</span><select className="field" name="categoryId" defaultValue={row.category_id}>{(categories??[]).filter(c=>c.kind===row.type).map(c=><option value={c.id} key={c.id}>{c.name}</option>)}</select></label></div><div className="grid grid-cols-2 gap-3"><label><span className="label">Ámbito</span><select className="field" name="scope" defaultValue={row.scope}><option value="shared">Compartido</option><option value="personal">Personal</option></select></label><label><span className="label">Privacidad</span><select className="field" name="privacy" defaultValue={row.privacy}><option value="visible">Visible</option><option value="private">Privado</option></select></label></div><SubmitButton>Guardar cambios</SubmitButton></form></section></div>}
