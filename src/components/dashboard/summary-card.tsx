export function SummaryCard({ label, value, tone = "green", detail }: { label: string; value: string; tone?: "green"|"coral"|"plain"; detail?: string }) { const bg = tone === "green" ? "bg-[#dceee6]" : tone === "coral" ? "bg-[#f7ddd4]" : "bg-[#fffdf7]"; return <article className={`rounded-2xl border border-black/5 p-5 ${bg}`}><p className="text-sm font-bold text-[#6c7f7a]">{label}</p><p className="mt-2 text-2xl font-black tracking-tight">{value}</p>{detail && <p className="mt-1 text-xs text-[#6c7f7a]">{detail}</p>}</article>; }
