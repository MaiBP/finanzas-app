export function SummaryCard({ label, value, tone = "green", detail }: { label: string; value: string; tone?: "green"|"coral"|"plain"; detail?: string }) { const bg = tone === "green" ? "bg-[#87cd64]" : tone === "coral" ? "bg-[#ff96be]" : "bg-[#73c8dc]"; return <article className={`rounded-sm border border-[#3a3434]/20 p-5 ${bg}`}><p className="text-xs font-bold uppercase tracking-wide text-[#6e6464]">{label}</p><p className="mt-2 text-2xl font-black tracking-tight">{value}</p>{detail && <p className="mt-1 text-xs text-[#6e6464]">{detail}</p>}</article>; }
