export function SummaryCard({ label, value, tone = "green", detail, compact = false }: { label: string; value: string; tone?: "green"|"coral"|"plain"|"lilac"; detail?: string; compact?: boolean }) { const bg = tone === "green" ? "bg-[#87cd64]" : tone === "coral" ? "bg-[#ff6e7d]" : tone === "lilac" ? "bg-[#e19bf5]" : "bg-[#7da0ff]"; return <article className={`rounded-sm border border-[#3a3434]/20 p-5 ${bg}`}><p className="w-fit bg-[#ffff50] px-1 text-xs font-bold uppercase tracking-wide text-[#3a3434]">{label}</p><p className={`mt-2 font-black tracking-tight ${compact?"text-lg leading-snug":"text-2xl"}`}>{value}</p>{detail && <p className="mt-1 w-fit bg-[#ffff50] px-1 text-xs text-[#3a3434]">{detail}</p>}</article>; }
